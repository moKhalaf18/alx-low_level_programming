#include "main.h"

/**
 * _isupper - check if a letter is upper
 *@x: The number to be checked
 * Return: 1 if its upper letter or 0 if any else
*/

int _isupper(int x);
{
	if (x >= 65 && x <= 90)
		return (1);
	else
		return (0);
}
