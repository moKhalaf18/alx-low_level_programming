#include <stdio.h>
#include "main.h"

/**
 * main - prints the number of arguments passed to the progra
 * @argc: numbur of arguments
 * @argv: array of argumrnts
 *
 * Return: always 0 (success)
 */
int main(char argc, char *argv[])
{
	void argv; /*ignore argv*/
        printf("%s\n", *argv -1);

        return (0);
}
